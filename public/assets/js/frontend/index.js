define(['jquery', 'bootstrap', 'frontend',], function ($, undefined, Frontend) {

    var Controller = {
        index: function () {
            console.log(Config.demo.name);
        }
    };
    return Controller;
    
});